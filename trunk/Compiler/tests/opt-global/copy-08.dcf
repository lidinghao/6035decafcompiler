class Program {
	void main() {
		int a, b, c;
		a = 5;
		b = a + 5;
		if (a < 5) {
			a = b;
			a = 2*c + 1;
		} else {
			a = b;
		}
		c = a; // should not change, 'a' is redefined in one execution path
	}
}