class Program {
	void main() {
		int a, b, c;
		a = 5;
		b = a + 5;
		if (a < 5) {
			a = b;
			b = 2*a + 1;
		} else {
			a = b;
		}
		c = a; // should not change, 'b' is redefined in one execution path
	}
}